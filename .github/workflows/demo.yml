name: Hello World

on:
  push:
    branches: [main]

jobs:               # this must be at the top level
  demo:
    runs-on: ubuntu-latest   # uses a hyphen, not a space

    steps:
      - name: Greetings
        run: echo "Hello, World!"

